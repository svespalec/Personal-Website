---
import { Icon } from 'astro-icon/components';
---

<section class="py-16 md:py-12">
  <div class="container mx-auto px-4">
    <div class="max-w-3xl">
      <h1 class="text-5xl md:text-7xl font-bold mb-6">
        <span class="text-white">Hello, I'm </span>
        <span class="bg-gradient-to-r from-purple-400 to-blue-400 bg-clip-text text-transparent animate-gradient">
          Samuel.
        </span>
      </h1>
      
      <p class="text-xl text-gray-400 mb-8 fade-in-up" data-aos="fade-up" data-aos-delay="200">
        As an experienced back-end developer and reverse engineer, I specialize in crafting robust and secure software applications. With a deep understanding of system architecture and a passion for cybersecurity, I excel in developing cutting-edge solutions that protect and optimize digital infrastructures.
      </p>
      
      <div class="flex gap-4">
        <a
          href="https://github.com/yourusername"
          class="flex items-center gap-2 px-6 py-3 rounded-full bg-white/5 text-white hover:bg-white/10 transition-all duration-300"
        >
          <Icon name="mdi:github" class="w-4 h-4" />
          View GitHub
        </a>
        <a
          href="/resume.pdf"
          class="flex items-center gap-2 px-6 py-3 rounded-full bg-purple-500/10 text-purple-300 hover:bg-purple-500/20 transition-all duration-300"
        >
          <Icon name="mdi:file-document" class="w-4 h-4" />
          Download Resume
        </a>
      </div>
    </div>
  </div>
</section>

<style>
  .animate-gradient {
    background-size: 200% auto;
    animation: shine 1.5s linear infinite;
    background-image: linear-gradient(
      to right,
      #818cf8 20%,
      #60a5fa 40%,
      #818cf8 60%,
      #60a5fa 80%
    );
  }

  @keyframes shine {
    to {
      background-position: 200% center;
    }
  }

  .fade-in-up {
    animation: fadeInUp 0.8s ease-out forwards;
    opacity: 0;
  }

  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
</style>